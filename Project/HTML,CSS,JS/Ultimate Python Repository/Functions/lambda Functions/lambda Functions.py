### Lambda Functions ###
  # Anonymous function - Defined without a name

# Function_Name = lambda Arguments: Expression
add = lambda x, y: x + y
# function body - can contain any number of arguments, 1 expression
print("Sum = ", add(3, 6)) # Output: Sum =  9
